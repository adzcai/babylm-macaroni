# Install a CUDA-matched torch FIRST (setup.sh does this), e.g.
#   pip install torch --index-url https://download.pytorch.org/whl/cu124
torch>=2.4
transformers>=4.44,<5      # the vendored trainer uses TrainingArguments(warmup_ratio=...), removed in 5.x
datasets>=2.20
huggingface_hub>=0.24
tokenizers>=0.19
accelerate>=0.33
pyyaml>=6.0
python-dotenv>=1.0
wandb>=0.17          # the trainer reports to W&B; train.py defaults to offline mode

# Numerics + tables + plotting.
numpy>=1.26
scipy>=1.11
pandas>=2.1
pyarrow>=16.0
matplotlib>=3.8

# Evaluation harness (babylm-eval drives lm-eval).
lm-eval>=0.4.3
scikit-learn>=1.3
seqeval>=1.2

# Word-level analyses (Figs 4, 8): lexicon mining, Chinese segmentation, POS tagging.
# After install:  python -m spacy download en_core_web_sm nl_core_news_sm
spacy>=3.7
jieba>=0.42
opencc-python-reimplemented>=0.1.7
stopwordsiso>=0.6
