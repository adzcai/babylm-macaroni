# Core modelling + data.
# NOTE: install a CUDA-matched torch FIRST (see setup.sh) — e.g.
#   pip install torch --index-url https://download.pytorch.org/whl/cu124
torch>=2.4
transformers>=4.44
datasets>=2.20
huggingface_hub>=0.24
tokenizers>=0.19
accelerate>=0.33

# Numerics + tables + plotting.
numpy>=1.26
scipy>=1.11
pandas>=2.1
pyarrow>=16.0
matplotlib>=3.8

# Evaluation harness (the babylm-eval multilingual track drives lm-eval).
lm-eval>=0.4.3
scikit-learn>=1.3
seqeval>=1.2

# Lexicon / word-set / POS builders for the alignment + trajectory figures
# (figures/_lib/{align,contextual,traj}): mixed-script tokenisation, Chinese
# segmentation, POS tagging, and function-word filtering. Imported lazily, so
# the plain measurement/plot paths stay importable without them.
# After install, fetch the two spaCy models used by the Fig-7 POS map:
#   python -m spacy download en_core_web_sm nl_core_news_sm
spacy>=3.7
jieba>=0.42
opencc-python-reimplemented>=0.1.7
stopwordsiso>=0.6
